#include <stdio.h>
/**
 * main - Showing the name of the file
 * @argc: Size of input array
 * @argv: Pointer to input array
 * Return: Always 0.
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
